# Variables d'environnement (optionnelles)
# Copier vers .env.local pour le développement local

# Environnement Node (défini automatiquement par Vercel en production)
# NODE_ENV=production

# Exemple pour une future API backend Ricash
# NEXT_PUBLIC_API_URL=https://api.ricash.com
